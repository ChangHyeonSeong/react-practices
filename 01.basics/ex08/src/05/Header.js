import React, { Component } from 'react';

      //default 하나만 export 함
export default class extends Component  { //unamed class
    render() {  //render: render() 와 같다 ,  render() 함수를 오버로딩한것이다
        return (
            <h2>App05</h2>
        );
    }
}